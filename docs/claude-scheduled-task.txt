TESTSTOCK AUTOPILOT — SCHEDULED CLAUDE TASK

Run this task once per hour.

FETCH INTEGRITY — CRITICAL:
- Use raw main-branch GitHub files as the canonical Teststock data source for Claude. Do not use GitHub Pages as the canonical machine-readable source because Pages/CDN deployment can lag.
- Canonical signal: https://raw.githubusercontent.com/SableWolphen/Teststock/main/docs/signal.json
- Canonical real-fill policy: https://raw.githubusercontent.com/SableWolphen/Teststock/main/docs/real-fill-learning-policy.txt
- Canonical journal template: https://raw.githubusercontent.com/SableWolphen/Teststock/main/docs/teststock-real-trade-journal-template.json
- When supported, append a unique cache-buster such as ?cb=<current-UTC-unix-ms> to each raw fetch.
- If a GitHub Pages copy disagrees with raw main, trust raw main for Teststock research/signal data. If raw main itself is stale beyond the encoded freshness rules, fail closed and open no new trade.
- Before claiming the generator is frozen, compare the fetched raw signal generatedAt against the current UTC time and re-fetch once with a new cache-buster.

1. Read the canonical raw signal.json above and follow its current hardRules, hardAccountFloor, capitalLadder, performanceScalingGuard, probabilityFirstPolicy, crossAssetOpportunityRanking, profitLock, circuitBreakers, tradeFrequencyGuard, correlationGuard, executionQuality, symbolProbationPolicy, exitAutomation, healthGuard, portfolioGuard, outcomeGuard, assetClassRouting, entries, stops, and targets exactly.
2. Read the canonical raw real-fill-learning-policy.txt above and apply it before any new entry. Maintain a persistent file named Teststock-Real-Trades.json in this Cowork project/task workspace using the canonical raw journal template above as its schema. Robinhood history is authoritative.
3. Reconcile the journal against Robinhood orders, fills, positions, closed positions, and available history before any new risk. Never infer fills. Unknown/partial records stay UNKNOWN/OPEN and can never justify more risk.
4. Verify journal persistence across runs with persistence.testNonce. If persistence fails, reconstruct relevant recent statistics from Robinhood history and never use missing persistence to increase risk.
5. Recompute real-fill performance overall and by setupType, riskCluster, and symbol. Weak realized R, win rate, slippage, or protection reliability may reduce size or block a bucket. Learning may only make risk more conservative.
6. FUNDING LOCK: use only money already inside the dedicated Robinhood Agentic account. Never initiate deposits, ACH, card transfers, wires, bank transfers, internal transfers, withdrawals, or any other money movement. The account may currently contain about $100; profits may be reused and later user-added money may be used, but only the user may fund it.
7. Read live Agentic-account equity, NON-MARGIN CASH BUYING POWER, positions, open orders, status, and reliable history.
8. MARGIN / BORROWED MONEY: DO NOT USE IT. Never borrow, create a margin debit, use cash advances, or increase buying power with debt.
9. HARD FLOOR: below $75 equity, manage exits only. New-entry eligibility may resume at $80+ only if every other guard passes.
10. Select the exact equity tier: >=0/<80 RECOVERY; >=80/<100 DEFENSE; >=100/<125 PROVE; >=125/<150 SCALE; >=150 GROWTH. Exactly $100 is PROVE.
11. Apply performanceScalingGuard after the equity tier. Equity alone cannot unlock higher risk; missing or weak real-fill evidence caps the effective risk tier.
12. Apply profitLock and count total planned stop-loss across all open positions plus the proposed trade. Never intentionally size below the protected-equity floor.
13. Apply circuit breakers using CONFIRMED ROBINHOOD HISTORY ONLY: if reliable Robinhood history shows daily loss >=3% or rolling 7-day drawdown >=6%, open no new positions and continue protective management. Never use docs/data/trade-history.json, shadow trades, scanner simulations, model outcomes, or synthetic budget records to trigger a live pause.
14. TRADE FREQUENCY: multiple qualified opportunities are allowed. Do not exceed 3 new Teststock positions in one calendar day, 10 new Teststock positions in rolling 7 days, 1 new option position per day, or 1 new crypto position per day. These are maxima, not targets. Never force trades to fill the quota.
15. PORTFOLIO CAP: up to 4 concurrent Teststock positions may be held only when each independently qualifies and total deployed cash, planned stop risk, correlation, event, execution, and protection limits all pass.
16. CORRELATION: do not stack the same risk cluster. Existing correlated exposure can make a new entry smaller or ineligible.
17. ENTRY-GATE ROBUSTNESS: inspect signal.entryGateRobustness. Use separate development, 2024-2025 untouched holdout, and 2026+ second untouched holdout evidence. A failed untouched regime blocks/reduces the affected regime. Sparse second-holdout evidence reduces risk; it never justifies more risk.
18. REGIME-SPECIFIC ENTRIES: apply the encoded stricter profile for TRENDING_UP, CALM, VOLATILE, TRENDING_DOWN, MIXED, or UNKNOWN. Never substitute one regime's easier conditions for another.
19. BREADTH: require the encoded breadth confirmation. Broad participation strengthens a candidate; weak breadth blocks/reduces it according to the plan.
20. SYMBOL PROBATION: lightly tested symbols start reduced. Historical sample depth and real Robinhood fills may promote them. Weak symbol-level REAL-FILL performance may reduce or block them. Scanner/model history may not block live trading by itself.
21. GAP RISK: when reliable prior regular-session close and current open exist, skip or wait for revalidation if the opening gap is >4% absolute or consumes >50% of the planned entry-to-stop distance. Never widen a stop to accommodate a gap.
22. LIQUIDITY: prefer tighter spreads, higher reliable dollar volume, and cleaner historical fills. Stock live spread above 0.35%, option above 10%, or crypto above 0.75% blocks a new entry.
23. TARGET2 REWARD/RISK: use the full target2 for the minimum reward/risk qualification gate. Target1 is a partial-profit/de-risking milestone, not the 2.5R qualification target.
24. COST-ADJUSTED EXPECTANCY: use the encoded costAdjustedConservativeExpectedR when available. Do not replace it with nominal expectancy and do not assume costs are zero.
25. CROSS-ASSET PRIORITY: inspect signal.crossAssetOpportunityRanking. Use it only to prioritize among already research-qualified STOCK, OPTION, and CRYPTO opportunities. A higher rank never overrides live account, entry, broker, event, real-fill, whole-contract, spread, gap, protection, cash, correlation, frequency, or risk rules.
26. Apply probabilityFirstPolicy after all base gates. Stocks are primary. A stock must satisfy the current minimum historical sample, win-rate, conservative expected-R, cost-adjusted expected-R, target2 reward/risk, quality, volatility, regime, breadth, symbol-probation, event, gap, liquidity, and real-fill restrictions.
27. EVENT BLACKOUT: obey encoded earnings/filing/corporate-action windows. Options use the stricter event window.
28. CRITICAL DEPENDENCY FAIL-CLOSED: before every entry verify signal freshness, dual-holdout/regime validation, probability overlay, cross-asset ranking, live Robinhood balances/positions/orders, journal reconciliation or reliable reconstruction, and required protective-exit capability. Missing/stale/contradictory/unknown critical data means no new position.
29. Keep STOCK, OPTION, and CRYPTO routing separate. Never substitute between classes.
30. If a signal is stale, expired, WAIT, WAIT_FOR_TRIGGER, WAIT_FOR_MARKET_OPEN, DO_NOT_TRADE, DO_NOT_CHASE, or otherwise non-actionable, do nothing for that asset class.
31. ENTRY PERMISSION: if the current Claude/Robinhood environment allows unattended order placement, automatically submit independently AUTO_BUY_ELIGIBLE entries that pass every live check. If the environment requires user confirmation for a buy, ask only for approval of that specific qualified entry; do not bypass or evade the confirmation requirement. Do not ask the user to calculate size, stop, or exit details.
32. STOCK: buy only independently AUTO_BUY_ELIGIBLE orders inside the live entry range. More than one stock may be opened when independently qualified and total portfolio/risk/frequency/correlation rules remain satisfied. Use the smallest applicable sizing cap from all rules.
33. OPTION: stocks/cash remain default. Before any option require the current real-fill evidence unlock plus all elite research gates, holdout/regime/event permission, whole-contract risk fit, acceptable spread/liquidity, exact contract availability, active tier permission, and protective-exit capability. If one whole contract is too large, skip it.
34. CRYPTO: crypto is optional. Require current real-fill evidence unlock plus the encoded A/A+ research gates, BTC context for altcoins, liquidity, risk, and runtime broker support. Only one crypto position may be open at a time.
35. Prefer limit orders where supported. Never pay above maximumEntry. Record observed order/fill/spread/dollar-volume/gap/slippage/protection telemetry; never invent values.
36. AUTOMATIC EXIT MANAGEMENT: after any confirmed stock or option fill, immediately create and verify the strongest supported persistent protective exit for the full live quantity. Exit management should run automatically whenever the broker/agent permissions allow it, including stop-loss execution, protective stop replacement, target1 de-risking, target2 profit-taking, and risk-reducing exits. If the platform itself requires confirmation for a particular sell action, request only that required confirmation and never leave a new position deliberately unprotected.
37. A new stock or option is not complete until the strongest supported persistent protective exit is created and verified for the full live quantity. If protection cannot be created or verified, do not open the position.
38. Do not assume bracket/OCO support. Verify remaining quantity and conflicting sell orders before partial profits or protective-order replacements.
39. At target1, protect gains and take partial profit when practical; then protect the remainder at breakeven or tighter without widening risk. At target2, prioritize taking profit on most or all remaining size unless a tighter trailing plan is explicitly encoded.
40. Never use margin, borrowed money, naked/undefined-risk options, short stock, leveraged crypto, averaging down, adding to losers, wider stops, duplicate positions, or duplicate open orders.
41. LIVE OUTCOME GUARD SOURCE: only confirmed Robinhood fills, realized P&L, and reliable Robinhood history may reduce size, lock options/crypto, or pause live entries. docs/data/trade-history.json, scanner outcomes, shadow results, simulated budget rows, and research backtests are diagnostic/research data only and must never directly trigger a live trading pause.
42. SHADOW DATA is diagnostic only. Never treat shadow results as real P&L or automatic permission to loosen live rules.
43. Re-read buying power/equity/positions after every fill and reconcile the journal after every confirmed entry/exit/cancel/protection change. Material Teststock/Robinhood disagreement means no new risk.
44. DAILY SUMMARY: once per day according to signal.dailySummaryPolicy, report account value, cash, positions, planned risk, realized P&L, trades today, rolling 7-day trades, current regime/breadth, top ranked opportunity, blocked setups, health, journal persistence, equity tier, and effective risk tier.
45. Never increase risk merely to make money faster or recover losses. Cash is valid.

No strategy can guarantee profit or prevent loss. Follow the platform's actual trading-confirmation requirements; never attempt to bypass them.
