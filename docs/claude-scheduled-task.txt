TESTSTOCK AUTOPILOT — SCHEDULED CLAUDE TASK

Run this task once per hour.

FETCH INTEGRITY — CRITICAL:
- Use raw main-branch GitHub files as the canonical Teststock data source for Claude. Do not use GitHub Pages as the canonical machine-readable source because Pages/CDN deployment can lag.
- Canonical signal: https://raw.githubusercontent.com/SableWolphen/Teststock/main/docs/signal.json
- Canonical real-fill policy: https://raw.githubusercontent.com/SableWolphen/Teststock/main/docs/real-fill-learning-policy.txt
- Canonical journal template: https://raw.githubusercontent.com/SableWolphen/Teststock/main/docs/teststock-real-trade-journal-template.json
- When supported, append a unique cache-buster such as ?cb=<current-UTC-unix-ms> to each raw fetch.
- If a GitHub Pages copy disagrees with raw main, trust raw main for Teststock research/signal data. If raw main itself is stale beyond the encoded freshness rules, fail closed and open no new trade.
- Before claiming the generator is frozen, compare the fetched raw signal generatedAt against the current UTC time and re-fetch once with a new cache-buster.

1. Read the canonical raw signal.json above and follow its current hardRules, hardAccountFloor, capitalLadder, performanceScalingGuard, probabilityFirstPolicy, crossAssetOpportunityRanking, profitLock, circuitBreakers, tradeFrequencyGuard, correlationGuard, executionQuality, symbolProbationPolicy, exitAutomation, runnerContinuationPolicy, fractionalProtectionPolicy, healthGuard, portfolioGuard, outcomeGuard, assetClassRouting, entries, stops, and targets exactly.
2. Read the canonical raw real-fill-learning-policy.txt above and apply it before any new entry. Maintain a persistent file named Teststock-Real-Trades.json in this Cowork project/task workspace using the canonical raw journal template above as its schema. Robinhood history is authoritative.
3. Reconcile the journal against Robinhood orders, fills, positions, closed positions, and available history before any new risk. Never infer fills. Unknown/partial records stay UNKNOWN/OPEN and can never justify more risk.
4. Verify journal persistence across runs with persistence.testNonce. If persistence fails, reconstruct relevant recent statistics from Robinhood history and never use missing persistence to increase risk.
5. Recompute real-fill performance overall and by setupType, riskCluster, and symbol. Weak realized R, win rate, slippage, protection reliability, or synthetic-stop reliability may reduce size or block a bucket. Learning may only make risk more conservative.
6. FUNDING LOCK: use only money already inside the dedicated Robinhood Agentic account. Never initiate deposits, ACH, card transfers, wires, bank transfers, internal transfers, withdrawals, or any other money movement. Profits may be reused and later user-added money may be used, but only the user may fund it.
7. Read live Agentic-account equity, NON-MARGIN CASH BUYING POWER, positions, open orders, status, and reliable history.
8. MARGIN / BORROWED MONEY: DO NOT USE IT. Never borrow, create a margin debit, use cash advances, or increase buying power with debt.
9. HARD FLOOR: below $75 equity, manage exits only. New-entry eligibility may resume at $80+ only if every other guard passes.
10. Select the exact equity tier: >=0/<80 RECOVERY; >=80/<100 DEFENSE; >=100/<125 PROVE; >=125/<150 SCALE; >=150 GROWTH. Exactly $100 is PROVE.
11. Apply performanceScalingGuard after the equity tier. Equity alone cannot unlock higher risk; missing or weak real-fill evidence caps the effective risk tier.
12. Apply profitLock and count total planned stop-loss across all open positions plus the proposed trade. Never intentionally size below the protected-equity floor.
13. Apply circuit breakers using CONFIRMED ROBINHOOD HISTORY ONLY: if reliable Robinhood history shows daily loss >=3% or rolling 7-day drawdown >=6%, open no new positions and continue protective management. Never use scanner/model history to trigger a live pause.
14. TRADE FREQUENCY: target up to the top four independently qualified stock opportunities, ranked best-first. Do not exceed 4 new Teststock positions in one calendar day, 12 new Teststock positions in rolling 7 days, 1 new option position per day, or 1 new crypto position per day. These are maxima, not targets.
15. PORTFOLIO CAP: target up to 4 concurrent independently qualified stock positions. Every one must pass all gates on its own, and total deployed cash, planned stop risk, correlation, event, execution, and protection limits must all pass. If fewer than four qualify, keep unused money in cash.
16. CORRELATION: do not stack the same risk cluster. Existing correlated exposure can make a new entry smaller or ineligible.
17. ENTRY-GATE ROBUSTNESS: inspect signal.entryGateRobustness. Use separate development, 2024-2025 untouched holdout, and 2026+ second untouched holdout evidence. A failed untouched regime blocks/reduces the affected regime.
18. REGIME-SPECIFIC ENTRIES: apply the encoded stricter profile for TRENDING_UP, CALM, VOLATILE, TRENDING_DOWN, MIXED, or UNKNOWN.
19. BREADTH: require the encoded breadth confirmation.
20. SYMBOL PROBATION: lightly tested symbols start reduced. Historical sample depth and real Robinhood fills may promote them.
21. GAP RISK: skip or wait for revalidation if the opening gap is >4% absolute or consumes >50% of the planned entry-to-stop distance. Never widen a stop.
22. LIQUIDITY: stock live spread above 0.35%, option above 10%, or crypto above 0.75% blocks a new entry.
23. TARGET2 REWARD/RISK: use target2 for the minimum reward/risk qualification gate. Target1 is a partial-profit/de-risking milestone.
24. COST-ADJUSTED EXPECTANCY: use costAdjustedConservativeExpectedR when available.
25. CROSS-ASSET PRIORITY: use signal.crossAssetOpportunityRanking only to prioritize already-qualified opportunities. Ranking never overrides live safety checks.
26. Apply probabilityFirstPolicy after all base gates. Stocks are primary.
27. EVENT BLACKOUT: obey encoded earnings/filing/corporate-action windows.
28. CRITICAL DEPENDENCY FAIL-CLOSED: before every entry verify signal freshness, validation, probability overlay, ranking, live Robinhood balances/positions/orders, journal reconciliation, and the applicable protection capability. Missing/stale/contradictory/unknown critical data means no new position.
29. Keep STOCK, OPTION, and CRYPTO routing separate.
30. If a signal is stale, expired, WAIT, WAIT_FOR_TRIGGER, WAIT_FOR_MARKET_OPEN, DO_NOT_TRADE, DO_NOT_CHASE, or otherwise non-actionable, do nothing for that asset class.
31. ENTRY PERMISSION: if the current Claude/Robinhood environment allows unattended order placement, automatically submit independently AUTO_BUY_ELIGIBLE entries that pass every live check. If the environment requires user confirmation for a buy, ask only for that specific qualified entry.
32. STOCK: evaluate stockPlan.stockOrders in rank order and attempt to establish up to four independently AUTO_BUY_ELIGIBLE stock positions when all live checks pass. Use the smaller live risk/cash caps. If rank #1 fails, evaluate the next qualified stock.
33. FRACTIONAL-SHARE PLATFORM LIMITATION: the connected Robinhood Agentic runtime has rejected stop-market and stop-limit trigger orders on fractional share quantities with `Invalid trigger for fractional order`. Do not repeatedly retry unsupported fractional trigger orders and do not claim one exists when it does not.
34. FRACTIONAL PROTECTION: determine live quantity after a fill. For whole-share quantity, place and verify a broker-resident persistent stop when supported. For a mixed position, protect whole shares with the broker stop and treat only the fractional remainder synthetically. For a position below one full share, protection is synthetic only.
35. SYNTHETIC STOP REQUIREMENTS: a NEW fractional stock entry is allowed only when an active market-session monitoring mechanism can be verified to check the position at intervals of 5 minutes or less, and planned stop risk is no more than 0.75% of current live equity. The once-per-hour scheduled task by itself does NOT satisfy this requirement. If no such active monitoring exists, skip the new fractional entry.
36. SYNTHETIC STOP EXECUTION: when a reliable live Robinhood price at or below the saved Teststock stop/invalidation is observed, immediately submit the supported fractional MARKET SELL for the protected fractional quantity when permissions allow. Then re-read orders/positions and verify the actual fill. Never infer a fill from submission.
37. SYNTHETIC MONITORING FAILURE: if active <=5-minute monitoring becomes unavailable during the regular session, do not open or add fractional exposure. Existing synthetic-only fractional exposure is DEGRADED/UNPROTECTED. If automatic risk reduction is permitted, prefer reducing/closing that synthetic-only quantity rather than knowingly leaving it unattended. Never pretend an hourly check is equivalent to a resting broker stop.
38. OPTION: stocks/cash remain default. Before any option require all elite gates, whole-contract fit, and persistent protective-exit capability.
39. CRYPTO: crypto is optional. Require all encoded A/A+ gates and runtime broker support.
40. Prefer limit orders where supported for entries. Never pay above maximumEntry. Record actual execution telemetry only when exposed.
41. AUTOMATIC EXIT MANAGEMENT: whole-share stock and supported option protection should use verified persistent broker orders. Fractional stock exits use the synthetic policy above. Reconcile sell quantities so broker stop orders plus fractional market sells can never exceed the live position.
42. Do not assume bracket/OCO support. Verify remaining quantity and conflicting sell orders before partial profits or protective-order replacements.
43. CONDITIONAL RUNNER EXIT: at target1 sell about 25% of the ORIGINAL stock position when practical. At target2 re-fetch the current signal. Strong continuation may leave at most 25%; moderate at most 15%; weak/stale/missing/blocked leaves no runner. A fractional runner additionally requires healthy <=5-minute synthetic monitoring; without it, leave no fractional runner.
44. RUNNER MANAGEMENT AFTER TARGET2: re-check continuation on every later management run. Protection may only tighten; never widen it to keep a runner alive.
45. Never use margin, borrowed money, naked/undefined-risk options, short stock, leveraged crypto, averaging down, adding to losers, wider stops, duplicate positions, or duplicate open orders.
46. LIVE OUTCOME GUARD SOURCE: only confirmed Robinhood fills, realized P&L, and reliable Robinhood history may reduce size, lock options/crypto, or pause live entries.
47. SHADOW DATA is diagnostic only.
48. Re-read buying power/equity/positions after every fill and reconcile the journal after every confirmed entry/exit/cancel/protection change. Material Teststock/Robinhood disagreement means no new risk.
49. DAILY SUMMARY: once per day report account value, cash, positions, planned risk, realized P&L, trades today, rolling 7-day trades, regime/breadth, top opportunity, blocked setups, health, journal persistence, equity tier, effective risk tier, and protection mode for each open stock (BROKER/HYBRID/SYNTHETIC/UNPROTECTED).
50. Never increase risk merely to make money faster or recover losses. Cash is valid.

No strategy, stop, synthetic trigger, model, or automation can guarantee profit or prevent loss. Fractional synthetic stops are materially less reliable than broker-resident stops because detection can be delayed and market sells can slip. Follow the platform's actual trading-confirmation requirements; never attempt to bypass them.
