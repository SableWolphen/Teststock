TESTSTOCK AUTOPILOT — LOW-CREDIT SCHEDULED TASK

Goal: do not spend Claude tokens polling prices or re-analyzing markets when nothing needs brokerage action.

CHEAP FIRST STEP — ALWAYS:
1. Fetch only:
   https://raw.githubusercontent.com/SableWolphen/Teststock/main/docs/data/trigger-board.json?cb=<current-utc-ms>
2. If monitorHealth is not OK, open no new risk. If executionNeeded=false, STOP immediately. Do not fetch the full signal. Do not query Robinhood. Do not analyze stocks/crypto. Do not produce a long report.
3. Only when executionNeeded=true, follow:
   https://raw.githubusercontent.com/SableWolphen/Teststock/main/docs/claude-trigger-task.txt

WHEN AN EVENT EXISTS:
- Fetch the fresh raw signal.json and execution-watchlist.json.
- Verify the latest main Teststock workflow is healthy and all signal dependencies are fresh.
- Query only the minimum live Robinhood data needed for the listed ticker/event.
- Robinhood is authoritative for cash, equity, positions, orders and fills.
- Use only non-margin cash already inside the dedicated account. Never initiate deposits, transfers, withdrawals or borrowing.
- BUY_TRIGGER is a wake signal only. Buy only if the current signal still says the setup is eligible and every live cash/risk/correlation/spread/gap/event/frequency/protection/drift guard passes.
- STOCK BUY PRIORITY: use signal.stockPlan.bestAcceptableEntryPolicy and the encoded entryTier on candidates. Try A/ELITE candidates first. If no A candidate survives the current live checks, try the highest-ranked B/BEST_ACCEPTABLE candidate at its reduced encoded size. If a candidate fails before order submission, continue through eligible fallbacks in the same execution run. Do not choose cash by preference when an A or B candidate still passes every hard guard.
- B-tier is not permission to weaken hard safety. Never relax funding lock, account floor, loss brakes, freshness, maximumEntry, spread, gap/chase, correlation/portfolio heat, protective-exit requirements, fractional-monitor requirements, margin/leverage restrictions, no-average-down, or no-widen-stop rules.
- After a confirmed buy, arm docs/data/execution-watchlist.json with only non-sensitive saved trigger levels. Fractional synthetic monitoring is not active until that write succeeds and the trigger-monitor workflow is healthy.
- TRIGGER_1_STOP: verify the live position, then use the strongest supported sell path immediately when permitted; fractional quantities use the supported market sell path when fractional triggers are unsupported. Verify the fill.
- TRIGGER_2_TARGET1: verify position/exit policy, execute the authorized partial sale, verify fill, mark target1Completed=true.
- TRIGGER_3_TARGET2: verify position/fresh runner authorization, execute the validated Target 2 sale, keep only an allowed protected runner, verify fill and update watchlist state.
- Never infer fills, widen stops, average down, chase, use margin, use external funding, or retry unsupported fractional trigger orders.

CREDIT RULE:
GitHub + Alpaca do the continuous price monitoring. Claude is for broker verification and order execution when the trigger board says action is needed.

LIMITATION:
The public repository cannot wake Claude by itself and cannot place Robinhood orders. If no authorized Claude run is active when a trigger appears, execution waits until the next Claude run. Do not describe this as instant or guaranteed protection. Fractional full-autopilot entries require a response path fast enough to satisfy the signal's current execution-latency rule.
