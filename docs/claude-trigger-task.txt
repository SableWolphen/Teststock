TESTSTOCK — STOCK EXECUTION TRIGGER TASK

1. Re-fetch raw-main execution-dispatch.json, trigger-board.json, signal.json, stock-tournament.json, and execution-watchlist.json.
2. Require healthy monitoring and matching current-generation identifiers before new risk.
3. Stops and validated Target 1/Target 2 exits have priority over buys. Verify the live Robinhood position/order state, execute the authorized risk-reducing action when permitted, verify the result, and update only non-sensitive watchlist state.
4. For a BUY_TRIGGER, use only the Stock Tournament winner and already-qualified fallbacks, A before B. Do not research or invent another stock.
5. Verify the candidate against live Robinhood price/range, spread, non-margin cash, equity/floor, positions/orders, trade-frequency, correlation, protection, and all current Teststock guards.
6. If the winner fails, try the next already-qualified Teststock fallback in the same run.
7. When one stock survives, ask the user to approve that exact ticker, amount, entry/limit, stop, and targets. Do not submit before approval.
8. After approval, submit only that order, verify the actual Robinhood state, arm the saved stop/targets in execution-watchlist.json, and stop.
9. Options and futures are disabled. Crypto is handled only by the separate direct Robinhood Crypto API lane using the Crypto Tournament champion/fallbacks.
10. Never infer fills, use margin, initiate funding, average down, widen stops, chase, or weaken a gate to force a trade.
